language: php

php:
  - "5.4"
  - "5.5"

services:
  - mongodb

before_script:
  - composer install
  - echo "extension = mongo.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini

script: phpunit --coverage-text
